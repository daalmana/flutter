import 'package:flutter/material.dart';

class AppShadows {
  static const boxShadow = BoxShadow(
    color: Color(0x80d7e4f9),
    offset: Offset(0, 18),
    blurRadius: 52.85371780395508,
    spreadRadius: 0,
  );

  AppShadows._();
}
